名称: git提交总结，
描述：指定用户名和天数，总结提交者的内容
参数描述：
	1.git记录中用户的名称，一个或者多个，多个用逗号隔开
	2.天数可以不输出，默认就是当天
输出结果：
	1.总结报告格式：用户名称-日期.md
	2.总结报告存放位置：当前仓库/.claude/git_commit_report/



名称：git_code_review
描述：指定用户名和天数，获取提交者的git记录，对每一个提交进行code review
参数描述：
	1.git记录中用户的名称，一个或者多个，多个用逗号隔开
	2.天数可以不输出，默认就是当天
输出：
	1.code review报告格式：用户名称-日期.md
	2.code review报告存放位置：当前仓库/.claude/git_code_review/
	3.code review报告首先以文件为单位对git commit进行总结，然后对每一行改动进行分析，给出建议。


名称：代码重构分析
描述：用户提供一个需求，分析仓库中的可能受此需求影响的代码行，输入一个待办事项列表报告，并在用户修改之后，检查报告中待办事项完成情况
限制：
	1. 相同需求情况下，用户第一个使用本skill，先生成报告，不进行待办事项检查
	2. 相同需求情况下，用户第二次使用本skill，先检查是否生成了报告，没有生成报告，重复第一个使用情况，有生成报告，检查报告中待办事项完成情况
输出：
	1. 报告文件格式：用户需求-日期.md
	2. 报告保存位置：当前仓库/.claude/code_refactor_report/


名称：api文档生成
描述：用户提供一个文件或者目录，读取里边的接口信息，生成api文档
限制：
	1. 生成api文档的需要符合OpenAPI规范，OpenAPI规范地址：https://swagger.io/specification/
	2. 生成的文档条目清晰，可读性强
输出：
	1. api文档格式：日期-时间.md
	2. api文档保存位置：当前仓库/.claude/api_doc/




编写一个创建rust应用脚手架的智能体，技术栈：
| 组件 | 推荐工具/Crate | 作用 |
     |------|----------------|------|
     | **后端核心** | Tauri v1.x | IPC 桥接、窗口管理、安全插件 |
     | **前端 UI** | Svelte/Vue/React + Tailwind/Vite | 声明式 UI，热重载开发 |
  | 参数解析 | `clap` v4.x | 声明式、子命令支持、自动生成帮助/补全。 |
  | 配置管理 | `config` 或 `serde + toml` | 多源配置（CLI/ENV/文件）。 |
  | 日志 | `tracing` + `tracing-subscriber` | 结构化日志，支持多级输出。 |
  | 序列化 | `serde` + `serde_json`/`toml` | 数据互转。 |
  | 错误处理 | `anyhow` + `thiserror` | 用户友好错误链。 |
  | 测试 | `cargo test` + `proptest`（属性测试） | 覆盖核心逻辑。 |
     | **异步运行时** | Tokio | 并发任务、网络 |
     | **HTTP/网络** | reqwest + serde | API 调用、JSON 处理 |
     | **数据库** | rusqlite / sled | SQLite 嵌入式 / KV 存储 |
     | **状态管理** | tauri-state / signals (前端) | 跨界共享状态 |
     | **构建/打包** | Cargo + tauri-cli | 一键跨平台分发 (.msi/.dmg/.deb) |
     | **其他** | tao (窗口) + wry (WebView) | 系统原生集成 |


添加一个示例需求，编写两个界面，一个编辑器界面，一个设置界面
编辑器界面布局：
	1. 左侧为目录导航，目录增加右键菜单，菜单选项为常用文件操作
	2. 右侧为编辑器，可以编辑左侧目录导航打开的文件,可以关闭打开的文件，按ctrl+s保存或者点击编辑器右下侧的保存，ctrl+shift+s保存全部或者点击编辑器右下侧的保存全部
	3. 编辑器下侧为多tab操作页，目前两个tab:
		3.1: 聊天输入tab: 
			3.1.1：上层左侧有个关联按钮，点击可以关联目录导航中的文件，被关联的文件在右侧向右排列显示
			3.1.2:   中层为输入框，可以输入多行文本，输入框右下侧有两个操作，左边清空聊天聊天框，右边发送将文本发到后端处理
			3.1.3： 下层为操作栏，从左到右，模型选择下拉框，code_cli选择下拉框
		3.2 输出tab: 实时输出cli日志，可以上下翻滚查看日志，可以进行日志搜索
		3.3 终端tab: 打开当前操作系统的终端。进行终端操作
设置布局：从上到下：
			1. 工作区切换
			2. 应用数据目录切换
			3. cli工具路径设置
			4. 环境变量设置（应用启动时临时设置，应用关闭时清除）
			5. 模型列表，可以增删改
			6. code_cli列表，可以增删改
		







